import * as React from 'react';
import TextField from "@material-ui/core/TextField";

const CoronaTextField: React.FunctionComponent<any> = (props) => {
    return (
        <TextField
            {...props}
        />  
    );
};

export default CoronaTextField;
